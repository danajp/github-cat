# `use devenv` is provided by devenv's direnv integration, which is not present
# in every user's global direnvrc. Load it on the fly so this .envrc works
# regardless of the developer's direnv setup.
eval "$(devenv direnvrc)"

use devenv
